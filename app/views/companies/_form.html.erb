<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h6><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h6>
      <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :company_name, class:"form-label" %>
    <%= f.text_field :company_name, class:"form-control" %>
  </div>
  <div class="mb-3">
    <%= f.label :company_address, class:"form-label" %>
    <%= f.text_area :company_address, class:"form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class:"btn btn-primary" %>
  </div>
<% end %>
